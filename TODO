bound the periodentry field?
make the difficulties disjoint?
add anki-like memory?
fix the graphics -- specifically, the button sizes?
add a speed slider instead of a period field, like in TA?
add a history window?
show the correct answers?
add more problem types... e.g., from algebra?
find a better modulus operator
